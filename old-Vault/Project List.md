­­­­­
```dataview
table name as "Name", estimated_price as "Estimated total", amas_funds as "Amas funds", expense as "Cumulated expense", current_budget as "Current budget"
where type = "project"
```
